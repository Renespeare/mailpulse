# MailPulse Docker Environment Configuration
# Copy this file to .env and update with your values for Docker deployment

# ⚠️ SECURITY: Change ALL default values for production!

# Database Configuration
POSTGRES_PASSWORD=changeme123

# Encryption (MUST be exactly 32 characters for AES-256)
ENCRYPTION_KEY=changeme-32-char-encryption-key

# Admin Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme-secure-admin-password
JWT_SECRET=changeme-32-char-jwt-secret-key

# Optional: Override default ports (uncomment if needed)
# SMTP_PORT=2525
# HTTP_PORT=8080

# Optional: Dashboard API URL override (uncomment if needed)
# VITE_RELAY_API_URL=http://relay:8080